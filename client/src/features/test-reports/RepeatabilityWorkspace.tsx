import { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, Check, Lock, Save } from 'lucide-react';
import { Link, useParams } from 'react-router-dom';
import axios from 'axios';
import './repeatability.css';
import TestProcedureInfo from '../guidance/TestProcedureInfo';

const units = ['mg', 'g', 'kg', 't'];
const display = (value: unknown, unit?: string) => Number.isFinite(Number(value)) ? `${new Intl.NumberFormat('en-IN', { maximumFractionDigits: 9 }).format(Number(value))}${unit ? ` ${unit}` : ''}` : '—';

export default function RepeatabilityWorkspace() {
  const { reportId = '' } = useParams();
  const [data, setData] = useState<any>(null); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  useEffect(() => { void axios.get(`/test-reports/${reportId}/repeatability`).then(response => setData(response.data)).catch(e => setError(e.response?.data?.message || 'Unable to load the repeatability test.')); }, [reportId]);
  const start = async () => { setBusy(true); try { const response = await axios.post(`/test-reports/${reportId}/repeatability/start`); setData((current: any) => ({ ...current, ...response.data })); setError(''); } catch (e: any) { setError(e.response?.data?.message || 'Unable to start the repeatability test.'); } finally { setBusy(false); } };
  if (!data) return <main className="repeatability-page"><div className="repeatability-loading">{error || 'Loading repeatability test…'}</div></main>;
  const { report, applicability, test } = data; const snapshot = test?.instrumentSnapshot || report?.instrument || {}; const unit = snapshot.unit || 'g'; const supported = applicability?.status === 'APPLICABLE' && applicability?.executionSupported !== false;
  if (!supported) return <main className="repeatability-page"><header className="repeatability-top"><Link to={`/tester/reports/${reportId}/testing`}><ArrowLeft size={16} /> Testing route</Link><strong>OIML R 76 A.4.10</strong></header><section className="repeatability-content"><div className="repeatability-heading"><span className="technical-label">TESTING · A.4.10</span><h1>Repeatability</h1><p>{applicability?.reason || 'Complete the instrument configuration before starting this procedure.'}</p></div><Link className="repeatability-secondary" to={`/tester/reports/${reportId}/testing`}>Back to Testing</Link></section></main>;
  if (!test) return <main className="repeatability-page"><header className="repeatability-top"><Link to={`/tester/reports/${reportId}/testing`}><ArrowLeft size={16} /> Testing route</Link><strong>OIML R 76 A.4.10</strong></header><section className="repeatability-content"><div className="repeatability-heading"><span className="technical-label">TESTING · A.4.10</span><h1>Repeatability</h1><p>Several weighings of the same load are compared using the applicable MPE.</p></div><section className="repeatability-start"><div><h2>Start the generated repeatability plan</h2><p>The target loads and repetition count are derived from the persisted instrument profile and control stage.</p></div><button className="repeatability-primary" disabled={busy} onClick={() => void start()}>Start Test</button></section></section></main>;
  return <main className="repeatability-page"><header className="repeatability-top"><Link to={`/tester/reports/${reportId}/testing`}><ArrowLeft size={16} /> Testing route</Link><div><strong>NAWI</strong><span>TEST &amp; REPORT SYSTEM</span></div><span>OIML R 76 A.4.10</span></header><section className="repeatability-content"><div className="repeatability-heading"><div><span className="technical-label">TESTING · A.4.10</span><h1>Repeatability</h1><p>The spread between repeated results of the same load must not exceed the absolute MPE.</p></div><span className={`repeatability-status ${String(test.result).toLowerCase()}`}>{test.result === 'NOT_DETERMINED' ? test.status : test.result}</span></div>{error && <div className="repeatability-error">{error}</div>}<section className="repeatability-context"><div><small>Control stage</small><strong>{test.controlStage}</strong></div><div><small>Accuracy class</small><strong>{snapshot.accuracyClass || '—'}</strong></div><div><small>Max</small><strong>{display(snapshot.max, unit)}</strong></div><div><small>Unit</small><strong>{unit}</strong></div><div><small>Automatic zero-setting / tracking</small><strong>{snapshot.zeroSettingMethod === 'Automatic' || snapshot.zeroTracking === true ? 'REQUIRED ON' : 'Not configured'}</strong></div></section><ProcedureConfirmation test={test} required={snapshot.zeroSettingMethod === 'Automatic' || snapshot.zeroTracking === true} busy={busy} onSaved={saved => setData((current: any) => ({ ...current, test: saved }))} onError={setError} /><section className="repeatability-series-progress" aria-label="Repeatability series">{test.series.map((series: any, index: number) => <div className={series.status === 'COMPLETED' ? 'complete' : series.status === 'AVAILABLE' || series.status === 'IN_PROGRESS' ? 'active' : 'locked'} key={series.seriesId}><span>{series.status === 'COMPLETED' ? <Check size={15} /> : series.status === 'LOCKED' ? <Lock size={14} /> : index + 1}</span><strong>{series.label}</strong><small>{display(series.targetLoad?.value, series.targetLoad?.unit || unit)} · {series.requiredRepetitions} weighings</small></div>)}</section><RepeatabilitySeries series={test.series.find((item: any) => item.status !== 'COMPLETED')} unit={unit} procedureConfirmed={Boolean(test.procedureConfirmation?.unloadedInstrumentRestConfirmed)} busy={busy} reportId={reportId} onSaved={saved => setData((current: any) => ({ ...current, test: saved }))} onError={setError} />{test.status === 'COMPLETED' && <section className="repeatability-complete"><Check size={22} /><div><h2>A.4.10 {test.result}</h2><p>All required repeat weighings were evaluated. The result spread uses maximum result minus minimum result.</p></div><Link className="repeatability-secondary" to={`/tester/reports/${reportId}/testing`}>Return to Testing</Link></section>}<section className="repeatability-trace"><span className="technical-label">TRACEABILITY</span><p>Source: OIML R 76-1:2006 §3.6.1 / Annex A A.4.10. Individual values use the shared A.4.4.3 calculation and the existing MPE rule engine.</p></section><TestProcedureInfo testId="A.4.10" configuration={{ ...snapshot, controlStage: test.controlStage }} /></section></main>;
}

function ProcedureConfirmation({ test, required, busy, onSaved, onError }: { test: any; required: boolean; busy: boolean; onSaved: (test: any) => void; onError: (message: string) => void }) {
  const saved = test.procedureConfirmation || {}; const [automatic, setAutomatic] = useState(saved.automaticZeroOnConfirmed === true); const [rest, setRest] = useState(saved.unloadedInstrumentRestConfirmed === true); const save = async (nextAutomatic: boolean, nextRest: boolean) => { try { const response = await axios.patch(`/test-reports/${window.location.pathname.split('/')[3]}/repeatability/procedure`, { automaticZeroOnConfirmed: nextAutomatic, unloadedInstrumentRestConfirmed: nextRest }); onSaved(response.data.test); } catch (e: any) { onError(e.response?.data?.message || 'Unable to save procedure confirmation.'); } };
  return <section className="repeatability-procedure"><div><span className="technical-label">PROCEDURE</span><h2>Repeat the same load</h2><ol><li>Load the instrument and take the reading when loaded.</li><li>Remove the load and wait until the unloaded instrument has come to rest.</li><li>If zero deviation occurs, reset the instrument to zero without determining a new zero error.</li></ol><p>Automatic zero-setting / zero-tracking: <strong>{required ? 'REQUIRED ON' : 'Not configured'}</strong></p></div><div className="repeatability-confirm"><label><input type="checkbox" checked={automatic} onChange={event => { setAutomatic(event.target.checked); if (event.target.checked) void save(true, rest); }} disabled={busy || automatic} /> {required ? 'I confirm automatic zero-setting / zero-tracking remained in operation.' : 'I confirm the configured zero condition for this test.'}</label><label><input type="checkbox" checked={rest} onChange={event => { setRest(event.target.checked); if (event.target.checked) void save(automatic, true); }} disabled={busy || rest} /> I confirm the unloaded instrument came to rest between weighings.</label><small>The confirmation records tester procedure observations; it does not prove them electronically.</small></div></section>;
}

function RepeatabilitySeries({ series, unit, procedureConfirmed, busy, reportId, onSaved, onError }: { series: any; unit: string; procedureConfirmed: boolean; busy: boolean; reportId: string; onSaved: (test: any) => void; onError: (message: string) => void }) {
  const [values, setValues] = useState({ actualLoad: '', loadUnit: unit, indication: '', indicationUnit: unit, deltaL: '', deltaLUnit: unit, notes: '' });
  if (!series) return null;
  const [saving, setSaving] = useState(false); const observations = series.observations || []; const repetition = observations.length + 1; const progress = `${observations.length} / ${series.requiredRepetitions}`; const latest = observations.at(-1); const preview = values.indication !== '' && values.deltaL !== '' && values.actualLoad !== '' ? 'Calculated after save' : '—';
  const set = (key: string, value: string) => setValues(current => ({ ...current, [key]: value }));
  const submit = async (complete: boolean) => { setSaving(true); try { const response = await axios.patch(`/test-reports/${reportId}/repeatability/series/${series.seriesId}/observations/${repetition}`, { actualLoad: Number(values.actualLoad), loadUnit: values.loadUnit, indication: Number(values.indication), indicationUnit: values.indicationUnit, deltaL: Number(values.deltaL), deltaLUnit: values.deltaLUnit, unloadedInstrumentAtRest: true, notes: values.notes, complete }); onSaved(response.data.test); setValues(current => ({ ...current, indication: '', deltaL: '', notes: '' })); } catch (e: any) { onError(e.response?.data?.message || 'Unable to save the repeatability observation.'); } finally { setSaving(false); } };
  return <section className="repeatability-series"><div className="repeatability-series-heading"><div><span className="technical-label">ACTIVE SERIES · {series.seriesId}</span><h2>{series.label}</h2><p>Target load: <strong>{display(series.targetLoad?.value, series.targetLoad?.unit || unit)}</strong> · required repetitions: <strong>{series.requiredRepetitions}</strong></p></div><strong>{progress} observations completed</strong></div><div className="repeatability-guidance"><span>Automatic zero-setting / zero-tracking remains on.</span><span>Each repetition uses the same actual load. Results and MPE are calculated automatically.</span></div>{observations.length > 0 && <div className="repeatability-table-wrap"><table><thead><tr><th>#</th><th>Actual load</th><th>Indication</th><th>ΔL</th><th>Result</th><th>MPE</th></tr></thead><tbody>{observations.map((item: any) => <tr key={item.repetition}><td>{item.repetition}</td><td>{display(item.actualLoad, item.unit)}</td><td>{display(item.indication, item.unit)}</td><td>{display(item.deltaL, item.unit)}</td><td>{item.individualResultStatus}</td><td>{display(item.mpeValue, item.mpeUnit)}</td></tr>)}</tbody></table></div>}<div className="repeatability-fields"><label>Actual load used<input type="number" min="0" step="any" value={values.actualLoad} onChange={event => set('actualLoad', event.target.value)} /><select value={values.loadUnit} onChange={event => set('loadUnit', event.target.value)}>{units.map(option => <option key={option}>{option}</option>)}</select></label><label>Indication I<input type="number" step="any" value={values.indication} onChange={event => set('indication', event.target.value)} /><select value={values.indicationUnit} onChange={event => set('indicationUnit', event.target.value)}>{units.map(option => <option key={option}>{option}</option>)}</select></label><label>Additional load ΔL<input type="number" min="0" step="any" value={values.deltaL} onChange={event => set('deltaL', event.target.value)} /><select value={values.deltaLUnit} onChange={event => set('deltaLUnit', event.target.value)}>{units.map(option => <option key={option}>{option}</option>)}</select></label><div className="repeatability-preview"><small>Individual result</small><strong>{preview}</strong></div><label className="repeatability-notes">Observation notes<textarea value={values.notes} onChange={event => set('notes', event.target.value)} /></label></div><div className="repeatability-actions"><button className="repeatability-secondary" disabled={busy || saving || !procedureConfirmed || !values.actualLoad || values.indication === '' || values.deltaL === ''} onClick={() => void submit(false)}><Save size={15} /> Save observation</button><button className="repeatability-primary" disabled={busy || saving || !procedureConfirmed || !values.actualLoad || values.indication === '' || values.deltaL === ''} onClick={() => void submit(true)}>Complete weighing {repetition}</button></div>{latest && series.status === 'COMPLETED' && <div className="repeatability-summary"><span>Minimum result <strong>{display(series.summary?.minimumResult, unit)}</strong></span><span>Maximum result <strong>{display(series.summary?.maximumResult, unit)}</strong></span><span>Repeatability range <strong>{display(series.summary?.repeatabilityRange, unit)}</strong></span><span>Absolute MPE <strong>{display(series.summary?.absoluteMpe, unit)}</strong></span><strong>{series.result}</strong></div>}</section>;
}
