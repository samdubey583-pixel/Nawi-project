{
  "name": "nawi-test-report-system",
  "private": true,
  "scripts": {"dev": "node scripts/dev.mjs", "install:all": "npm install && npm install --prefix server && npm install --prefix client"},
  "devDependencies": {"concurrently": "^9.1.2"}
}
