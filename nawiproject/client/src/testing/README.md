# Frontend testing domain

Tester-facing OIML execution work is grouped by clause under `a4`, `a5`, and `a6`. Shared procedure guidance lives in `guidance`; reusable cross-feature UI remains under `client/src/shared`.
