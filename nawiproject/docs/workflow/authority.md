# Authority workflow (planned)

Authority functionality is reserved for a future `client/src/features/authority` and `server/src/modules/authority` domain. Planned actions include Approve, Request Changes, Require Retest, and Reject. No Authority UI or API is implemented here.
