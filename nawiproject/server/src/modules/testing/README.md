# Backend testing domain

`engine` owns applicability and test routing. `calculations` owns shared metrological calculations. Clause-specific persistence and services live under `a4`, `a5`, and `a6`.
